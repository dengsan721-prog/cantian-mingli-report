# 参天风格命理报告 Demo

这是一个纯静态命理报告生成 demo。打开 `index.html` 即可使用，无需安装依赖。

## 功能

- 输入姓名、性别、历法、生日、时辰、出生地。
- 若命中内置种子资料，优先使用已校验盘面。
- 若为新资料，生成模型版报告，并标注待精排项与置信度。
- 报告结构采用 `work/mingli-system/report_model.md` 中定义的最高规格模型。
- 支持打印报告。

## 边界

当前 demo 是前端原型，内置轻量排盘估算与报告模型。生产版建议接入后端排盘服务，以完成农历换算、精确节气、真太阳时、四柱、大运和紫微等计算。

